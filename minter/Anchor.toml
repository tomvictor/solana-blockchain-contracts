[toolchain]

[features]
resolution = true
skip-lint = false

[programs.localnet]
minter = "8wM74RmqGofuS5SeCiNPakXWF5MHvEEh3R4A2igZeaeT"

[registry]
url = "https://api.apr.dev"

[provider]
# cluster = "Localnet"
cluster = "Devnet"
wallet = "~/.config/solana/id.json"

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"
